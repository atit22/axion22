
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name}}-telemetry-query

spec:
  selector:
    app: {{ .Release.Name}}-telemetry-query
  type : LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
 